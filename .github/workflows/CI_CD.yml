name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # Setup .NET SDK
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 9.0.x

      # Restore dependencies
      - name: Restore dependencies
        run: dotnet restore ./URLShortenMVC/URLShortenMVC.sln

      # Build project
      - name: Build
        run: dotnet build --configuration Release --no-restore ./URLShortenMVC/URLShortenMVC.sln

      # Run tests
      - name: Test
        run: dotnet test --no-build --verbosity normal ./URLShortenMVC/URLShortenMVC.sln

      # Build Docker image
      - name: Build Docker Image
        run: docker build -t minhhung55217/urlshortenmvc:latest ./URLShortenMVC

      # Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: minhhung55217
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Push Docker image to Docker Hub
      - name: Push Docker Image
        run: docker push minhhung55217/urlshortenmvc:latest
